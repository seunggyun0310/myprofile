<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="10dp"
        android:color="#00ffffff" /> <!-- 테두리 선의 두께와 색상 설정 -->

    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp" /> <!-- 내부 여백 설정 -->
    <size
        android:width="296dp"
        android:height="64dp" /> <!-- 사각형의 크기 설정 -->
        <solid android:color="#FFFFFF" /> <!-- 색상 지정 -->
        <corners android:radius="8dp" /> <!-- 모서리의 반지름 지정 -->

</shape>
